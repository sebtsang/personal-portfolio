# Optional: override the Ollama endpoint. Defaults to http://localhost:11434/v1.
# Set this to your Cloudflare Tunnel URL (or similar) if you deploy to Vercel
# and want the production site to reach Ollama on your desktop.
# OLLAMA_BASE_URL=http://localhost:11434/v1
